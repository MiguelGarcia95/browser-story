const Story = require('../models/Story');
const StoryMap = require('../models/StoryMap');

module.exports = {
  index: async (req, res) => {

  },

  edit: async (req, res) => {

  },
  
  addOption: async (req, res) => {
  
  },
}

